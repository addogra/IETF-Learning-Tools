Author: Aditya Dogra

# Core Beliefs

1. User input should not hard-fail if recoverable (suggestions before exit).
2. External web content is unstable; parsing must degrade gracefully.
3. Every user-visible feature requires automated regression tests.
4. Operational defaults should be safe and simple (minimal required config).
5. Existing behavior must not regress when adding new options.
